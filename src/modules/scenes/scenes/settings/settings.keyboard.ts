import {buildTelegrafKeyboard} from '../../helpers/buildTelegrafKeyboard';
import locales from '../../locales/ru.json';

export const settingsMainMenu = buildTelegrafKeyboard([[locales.keyboards.settings.aliases]]);

export default {settingsMainMenu};
